// import { getPath } from "@utils/get_path";

// const fs = require("fs");

// const filePath = (id) =>
//   `${getPath(__dirname).rootParentPath}/secret/google_auth/${id}`;

// export function storeToFile(text, id) {
//   fs.writeFile(filePath(id), text, (err) => {
//     if (err) {
//       console.error("Error writing to file:", err);
//     } else {
//       console.log("Text stored successfully!");
//     }
//   });
// }
